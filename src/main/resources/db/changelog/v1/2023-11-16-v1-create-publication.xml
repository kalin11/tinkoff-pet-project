<databaseChangeLog
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <changeSet id="2023-11-16-v1-create-publication" author="Artem Lysenko">

        <preConditions>
            <not>
                <tableExists tableName="publication"/>
            </not>
        </preConditions>
        
        <createTable tableName="publication">

            <column name="id" type="serial" autoIncrement="true">
                <constraints primaryKey="true"/>
            </column>

            <column name="description" type="text">
                <constraints nullable="true"/>
            </column>

            <column name="subject_topic_id" type="bigint">
                <constraints foreignKeyName="post_subject_topic_id_fkey" nullable="false"/>
            </column>

            <column name="created_at" type="timestamp">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <sql>
            alter table publication
            add constraint publication_description_check check ( length(description) > 0 );
        </sql>
        
    </changeSet>

</databaseChangeLog>